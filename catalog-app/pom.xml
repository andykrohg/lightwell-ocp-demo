<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.7.18</version>
        <relativePath/>
    </parent>

    <groupId>com.redhat.demo</groupId>
    <artifactId>catalog-app</artifactId>
    <version>1.0.0</version>
    <name>Lightwell Catalog Demo</name>
    <description>Sample catalog app demonstrating Lightwell Network integration</description>

    <properties>
        <java.version>17</java.version>
        <woodstox.version>6.0.3</woodstox.version>
        <jsonpath.version>2.7.0</jsonpath.version>
        <orgjson.version>20220320</orgjson.version>
        <!-- Pin all Spring Framework modules to 5.3.18 so spring-core CVEs are visible -->
        <spring-framework.version>5.3.18</spring-framework.version>
        <spring-core.version>${spring-framework.version}</spring-core.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>com.fasterxml.woodstox</groupId>
            <artifactId>woodstox-core</artifactId>
            <version>${woodstox.version}</version>
        </dependency>

        <dependency>
            <groupId>com.jayway.jsonpath</groupId>
            <artifactId>json-path</artifactId>
            <version>${jsonpath.version}</version>
        </dependency>

        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>${orgjson.version}</version>
        </dependency>

        <!-- Explicit spring-core dep so the remediated profile can override just this module -->
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-core</artifactId>
            <version>${spring-core.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>vulnerable</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <app.profile>vulnerable</app.profile>
            </properties>
        </profile>

        <profile>
            <id>remediated</id>
            <properties>
                <app.profile>remediated</app.profile>
                <woodstox.version>6.0.3.rhlw-00001</woodstox.version>
                <jsonpath.version>2.7.0.rhlw-00001</jsonpath.version>
                <orgjson.version>20220320.0.0.rhlw-00003</orgjson.version>
                <!-- Only override spring-core, not all spring-framework modules -->
                <spring-core.version>5.3.18.rhlw-00003</spring-core.version>
            </properties>
            <repositories>
                <repository>
                    <id>lightwell</id>
                    <name>Red Hat Lightwell Network</name>
                    <url>https://packages.redhat.com/lightwell/public-lightwell-demo/java/remediated/</url>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                </repository>
            </repositories>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
                <version>2.7.11</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>makeAggregateBom</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <projectType>application</projectType>
                    <schemaVersion>1.5</schemaVersion>
                    <includeBomSerialNumber>true</includeBomSerialNumber>
                    <includeCompileScope>true</includeCompileScope>
                    <includeProvidedScope>true</includeProvidedScope>
                    <includeRuntimeScope>true</includeRuntimeScope>
                    <includeSystemScope>true</includeSystemScope>
                    <outputFormat>json</outputFormat>
                    <outputName>bom</outputName>
                </configuration>
            </plugin>
        </plugins>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
    </build>
</project>
