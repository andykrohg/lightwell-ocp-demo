server:
  port: 8080

app:
  profile: "@app.profile@"
  version: "@project.version@"

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
