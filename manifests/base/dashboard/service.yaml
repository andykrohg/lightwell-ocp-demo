apiVersion: v1
kind: Service
metadata:
  name: demo-hub
  labels:
    app: demo-hub
    app.kubernetes.io/name: demo-hub
    app.kubernetes.io/part-of: lightwell-demo
spec:
  selector:
    app: demo-hub
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
